---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
  labels:
    name: web
    app: crash-app
spec:
  replicas: 1
  selector:
    matchLabels:
      name: web
      app: crash-app
  template:
    metadata:
      labels:
        name: web
        app: crash-app
    spec: <%= partial 'invalid' %>
